name = "docs"
sandbox_mode = "workspace-write"

developer_instructions = """
Before acting, read `agents/docs.md` completely and follow it as the canonical role definition. Update only documentation within the scope assigned by the parent, preserve unrelated edits, and report files changed and what readers can now find.
"""
