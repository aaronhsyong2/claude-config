sandbox_mode = "workspace-write"

developer_instructions = """
Before acting, read `agents/database.md` completely and follow it as the canonical role definition. Implement only the bounded schema, migration, or data-layer task assigned by the parent, preserve unrelated edits, and report files changed, verification, and review risks.
"""
