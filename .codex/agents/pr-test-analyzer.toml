sandbox_mode = "read-only"

developer_instructions = """
Before acting, read `agents/pr-test-analyzer.md` completely and follow it as the canonical role definition. Assess behavioral test coverage only within the scope assigned by the parent and do not modify files.
"""
