---
description: Build a fully-local, self-contained interactive HTML doc/plan/recap from Markdown.
argument-hint: "[doc|plan|recap] <topic, path, or scope>"
---

Use the visual-docs skill for this task. It is fully local: author a
Markdown-superset source and compile it to one self-contained, offline HTML file
(no server, no hosted dependency). Pick the mode from the request — `doc`
(default, including complex multi-file specs), `plan`, or `recap` — and treat any
arguments as the topic, source path, or scope:

$ARGUMENTS
